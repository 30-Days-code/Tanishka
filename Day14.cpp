/*Write a program using C++ template in which 
you have to create a function that accepts
both character type or integer type arrays, 
respectively, and prints the content of the
array as output. [ NOTE : Use only ONE variable
to accept for both the type of arrays (No two separate variables) ]*/
